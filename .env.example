# Copy to .env and fill in, then export before running (or use a tool that loads .env).
# NEVER commit a real .env file.

DISCORD_BOT_TOKEN=
DISCORD_BOT_API_KEY=
PANEL_BASE_URL=https://your-panel.example.com
